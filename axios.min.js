var app = new Vue({
  el: "#app",
  data: {
    bpi: null
  },
  mounted: function () {
    axios.get('https://api.coindesk.com/v1/bpi/currentprice.json')
      .then(function (response) {
        // console.log(response.data.bpi)
        // ビットコイン価格を取得してプロパティにデータを格納
        this.bpi = response.data.bpi
      }.bind(this))
      .catch(function (error) {
        console.log(error)
      })
  }
})
